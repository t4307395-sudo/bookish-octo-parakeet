# منصة الطلاب — v3 (كل حاجة دمجت في نسخة واحدة نهائية)

الملف ده بيوثّق إيه اللي اتضاف/اتغيّر لما دمجنا كل النسخ المتفرقة (ملفات لوحدها +
مشروع v2 كامل) في مشروع واحد نهائي.

## ✅ اللي كان ناقص وبني دلوقتي

| الميزة | الصفحة | الـ API |
|---|---|---|
| **تدريب حر** بفئات | `practice.html` | `GET /api/practice`, `GET /api/practice/categories`, `POST /api/practice/check` |
| **سؤال اليوم** (نفسه لكل الطلاب، بيتغير يومياً تلقائي) | `daily.html` | `GET/POST /api/daily` |
| **سجل اختباراتي** | `history.html` | `GET /api/history` |
| **الساحة العامة** (دردشة عامة مفتوحة للكل) | `community.html` | امتداد لـ `/api/chat/global/global` |
| **بلاغات الأسئلة** (طالب يبلّغ عن سؤال فيه مشكلة) | زرار 🚩 في `practice.html` و`daily.html` | `POST /api/questions/:id/report` |
| **لوحة بلاغات للأدمن** | قسم جديد في `admin.html` | `GET /api/admin/reports`, `POST /api/admin/reports/:id` |

## 🔧 ترقيات على ملفات موجودة (كانت النسخة الأحدث لوحدها، دمجناها هنا)

- `schema.sql`: إضافة `daily_answers` و`question_reports`.
- `style.css`: إضافة استايل `.period-tab.active`.
- `hub.html`: إضافة روابط تدريب حر/سؤال اليوم/سجل الاختبارات/الساحة العامة.
- `leaderboard.html` + `functions/api/leaderboard/index.js`: فلترة بالفترة (كل الوقت/أسبوع/شهر).
- `functions/api/chat/[roomType]/[roomId].js`: إضافة `roomType='global'` (أي حد يقرأ، لازم حساب يكتب).

## ⚠️ خطوة واحدة قبل التشغيل

قاعدة البيانات لازم تاخد كل محتوى `schema.sql` الجديد (خصوصاً جزء "Platform v3" في
آخر الملف). لو قاعدة بياناتك شغالة بالفعل، افتح D1 Console وشغّل الجزء الجديد بس
(أسفل تعليق `Platform v3`)؛ كله `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`
آمن يتنفذ من غير ما يأثر على بياناتك الحالية.

## 🧭 حاجات ممكن تتضاف بعدين (مش موجودة دلوقتي)

- سؤال اليوم حالياً بيتحدد من كل بنك الأسئلة (`bank` + كل الفئات) - لو عايز فئة
  ثابتة لسؤال اليوم (مثلاً رياضيات بس) سهل نظبطها.
- بلاغات الأسئلة دلوقتي بس "تم المراجعة" يدوي من الأدمن - مفيش تعديل مباشر
  للسؤال من نفس الشاشة (لسه بيحصل من D1 Console أو رفع تاني).
- التدريب الحر مفيهوش ترتيب زمني أو حفظ تقدم بين الجلسات (كل جولة مستقلة).
